import 'package:flutter/material.dart';

class SetAccountTypeScreen extends StatelessWidget {
  const SetAccountTypeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
